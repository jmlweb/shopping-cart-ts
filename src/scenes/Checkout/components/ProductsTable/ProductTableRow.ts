import styled from 'styled-components';

const ProductTableRow = styled.li`
  position: relative;
  display: grid;
  grid-template-columns: 45fr 20fr 20fr 15fr;
  align-items: center;
`;

export default ProductTableRow;
