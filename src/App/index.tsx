import { FC } from 'react';

import ShoppingCart from '../scenes/ShoppingCart';
import './App.css';

// In a real situation, the scene would be provided by a routing mechanism
const App: FC = () => <ShoppingCart />;

export default App;
